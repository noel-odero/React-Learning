import React from "react";
export const FamilyContext = React.createContext()
// react gives us a context that we hold in the above variable and making available to export so that it is imported elsewhere and we get the context

